#include <stdio.h>
#include <stdlib.h>

void main()
{
    int i;
    for (i=0;i%2;i++){

        // apenas lembre que o falso é sempre o 0.
        // i mod 2 é zero, portanto falso, logo não faz
        printf("O valor de i: %d\n", i);

    }
}
